name: migrate_jhunt_skill_gap_note
description: 'Migrate jhunt-skill-gap-note: identity migration (unchanged)'
depends_on: []
idempotent: true
source_match: 'match $x isa jhunt-skill-gap-note;

  fetch { "id": $x.id };'
target_insert: insert $x isa jhunt-skill-gap-note, has id $id;
skolem_keys:
- id
